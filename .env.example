# Dheme API Key — Server-side (optional)
# When set, tenant themes are fetched from the live Dheme API.
# When empty, pre-generated mock themes are used.
DHEME_API_KEY=

# Dheme API Key — Client-side (optional)
# Enables the floating theme customizer on each tenant page.
# When empty, the customizer shows a prompt to create a Dheme account.
NEXT_PUBLIC_DHEME_API_KEY=

# Dheme Base URL — Server-side (for OAuth token exchange)
# Default: https://dheme.com
# For local development: http://localhost:3005
DHEME_BASE_URL=

# Dheme Base URL — Client-side (for OAuth redirect)
# Default: https://dheme.com
# For local development: http://localhost:3005
NEXT_PUBLIC_DHEME_BASE_URL=
